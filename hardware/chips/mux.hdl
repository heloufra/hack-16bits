/** 
 * Multiplexor:
 * if (sel = 0) out = a, else out = b
 */
CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    Not(in=sel , out=aSel );
    And(a=a , b=aSel , out=aOut );
    And(a=b , b=sel , out=bOut );
    Or(a=aOut , b=bOut , out=out );
}
